# Local Dataverse instance from accel-compose.
DATAVERSE_HOST=http://127.0.0.1:8081

# Add a Dataverse API key before running integration tests.
DATAVERSE_API_KEY=

# Dataverse alias used by the local accel-compose test instance.
DATAVERSE=root
